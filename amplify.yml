version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - echo "Node.js backend - no build step needed"
  artifacts:
    baseDirectory: public
    files:
      - index.html
  cache:
    paths:
      - node_modules/**/* 