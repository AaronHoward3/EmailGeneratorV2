version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - echo "Node.js backend - no build step needed"
  artifacts:
    baseDirectory: .
    files:
      - src/**/*
      - lib/**/*
      - package.json
      - yarn.lock
      - amplify.yml
      - .env.example
  cache:
    paths:
      - node_modules/**/* 